# AI Builder Configuration
# Add these variables to your .env.local file

# ============================================
# GLM-4.7 API Configuration
# ============================================
# Your GLM API key from https://open.bigmodel.cn/
GLM_API_KEY=b173f65bddf643859165f424036f20e8.SBNisaIvJgVEmyeh

# ============================================
# GitHub Integration (for Direct Deployment)
# ============================================
# GitHub Personal Access Token (PAT)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_PAT_TOKEN=your_github_pat_token_here

# Your GitHub username (repository owner)
GITHUB_OWNER=your_github_username

# Repository name (default: EastD)
GITHUB_REPO=EastD

# Branch to deploy to (default: main)
GITHUB_BRANCH=main

# ============================================
# Vercel Deployment (Optional)
# ============================================
# Optional: Vercel deploy hook URL for manual triggers
# Create at: Vercel Dashboard → Project → Settings → Git → Deploy Hooks
VERCEL_DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/...

# ============================================
# Instructions
# ============================================
# 1. Copy these variables to your .env.local file
# 2. Replace placeholder values with your actual credentials
# 3. Never commit .env.local to git (it's already in .gitignore)
# 4. Restart your development server after adding variables

# ============================================
# GitHub PAT Setup Instructions
# ============================================
# 1. Go to: https://github.com/settings/tokens
# 2. Click "Generate new token" → "Generate new token (classic)"
# 3. Name it: "EastDock AI Builder"
# 4. Select scopes:
#    - ✓ repo (Full control of private repositories)
#    - ✓ workflow (Update GitHub Action workflows)
# 5. Generate token and copy it immediately (you won't see it again!)
# 6. Paste it as GITHUB_PAT_TOKEN value above
# 7. If your repo has branch protection on 'main', you'll need to either:
#    - Disable branch protection, OR
#    - Add the PAT to the "Allow specified actors to bypass required pull requests" list

# ============================================
# Security Notes
# ============================================
# - The AI Builder only modifies UI files (components, pages, styles)
# - API routes, config files, and sensitive files are protected
# - All deployments go through approval before pushing to GitHub
# - Keep your .env.local file secure and never share it
